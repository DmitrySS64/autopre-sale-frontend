import type {IModal, IModalShowParams} from "@widgets/modal/interface";

const createProjectModal : IModalShowParams = () => {
    return {
        title,
        canClose: true,
        content: (
            <>
                </>
        )
    }
}